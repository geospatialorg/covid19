export const environment = {
  apiUrl: '/api',
  production: false,
  data_refresh: 30 * 60 * 1000,
  enable_service_worker: false, // enable service worker
  enable_notifications: true, // enable / disable notification request
  enable_pwa_mobile_install: false, // enable pwa prompt install install
  notification_prompt_delay: 7000, // Allow notification request delay
  pwa_install_prompt_delay: 7000, // Prompt install delay
};
